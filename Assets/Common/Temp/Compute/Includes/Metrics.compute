/*
* Help ComputeShader
*/

static const uint numThreads = 8;

RWStructuredBuffer<float4> weight;
int pointsPerChunk;
float boundsSize;
float3 centre;
float3 offset;
float spacing;
float3 worldSize;
int _LODSize;
float _LodScaleFactor;
int _Scale;

int indexFromCoord(int x, int y, int z)
{
    return x + pointsPerChunk * (y + pointsPerChunk * z);
}